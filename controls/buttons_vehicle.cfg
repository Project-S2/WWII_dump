bindaxis_override A_RTRIGGER VA_FORWARD MAP_SQUARED
bind_override BUTTON_LTRIG "+back"

// Note that regular overrides get subverted by the new bind_override_passthrough
//  - If the action mapped by a player control has passthrough and that player control is remapped onto a button we otherwise override, the override will be missed
// -- We'll want a better solution in the future, but for now can use the passthroughs wholesale because we have no generic vehicle actions used anywhere in game
//bind_override BUTTON_A "+gearshiftdown"
//bind_override BUTTON_B "+handbrake"
bind_override_passthrough "+usereload"
//bind_override BUTTON_Y "+gearshiftup"

bind_override_passthrough "+smoke"

bind_override_passthrough "togglemenu"
bind_override_passthrough "togglescores"

bind_override_passthrough "+actionslot 1"
bind_override_passthrough "+actionslot 2"
bind_override_passthrough "+actionslot 3"
bind_override_passthrough "+actionslot 4"

bind_override_passthrough "+forward"
bind_override_passthrough "+back"
bind_override_passthrough "+moveleft"
bind_override_passthrough "+moveright"
bind_override_passthrough "+left"
bind_override_passthrough "-left"
bind_override_passthrough "+right"
bind_override_passthrough "-right"
bind_override_passthrough "+lookup"
bind_override_passthrough "-lookup"
bind_override_passthrough "+lookdown"
bind_override_passthrough "-lookdown"

//==========================================================================================
// Associate real axis to virtual axis.
// Real axis are    : A_LSTICK_Y, A_LSTICK_X, A_RSTICK_X, A_RSTICK_Y, A_RTRIGGER, A_LTRIGGER
// Virtual axis are : VA_SIDE, VA_FORWARD, VA_UP, VA_YAW, VA_PITCH, VA_ATTACK
//==========================================================================================
//bindaxis_override A_LSTICK_X VA_SIDE MAP_SQUARED
bindaxis_override A_PASSTHROUGH VA_SIDE

//bindaxis_override A_RSTICK_X VA_YAW MAP_LINEAR
//bindaxis_override A_RSTICK_Y VA_PITCH MAP_LINEAR
bindaxis_override A_PASSTHROUGH VA_YAW
bindaxis_override A_PASSTHROUGH VA_PITCH

//==========================================================================================
// Keyboard
//==========================================================================================
